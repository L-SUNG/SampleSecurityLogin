{{>layout/header}}
<div class="d-flex justify-content-center mt-2">
    <div class="row mx-0" style="width: 800px;">
        <form action="/member/signup" method="post">
            <div>
                <h1>유저등록</h1>
            </div>

            {{#validationErrors}}
            <div class="alert alert-danger" role="alert">
                <span>{{errorMsg}}</span>
            </div>
            {{/validationErrors}}

            <div class="form-group mt-3">
                <label for="id" class="form-label">아이디</label>
                <input type="text" class="form-control is-invalid" id="id" name="id" placeholder="아이디" required>
                <div class="valid-feedback">
                    사용가능한 아이디 입니다.
                </div>
                <div class="invalid-feedback">
                    <span id="invalidId">4글자 이상 50글자 이하로 작성해 주세요.</span>
                </div>
            </div>

            <div class="form-group  mt-3">
                <label for="pass">패스워드</label>
                <input type="password" class="form-control is-invalid" id="pass" name="pass" placeholder="패스워드" required>
                <div class="valid-feedback">
                    사용가능한 패스워드 입니다.
                </div>
                <div class="invalid-feedback">
                    최소 6자리이상의 1개 이상의 영대문자, 영소문자, 숫자를 사용하여 작성해 주세요.
                </div>
            </div>

            <div class="form-group  mt-3">
                <label for="passConfirm">패스워드 확인</label>
                <input type="password" class="form-control is-invalid" id="passConfirm" placeholder="패스워드 확인" required>
                <div class="valid-feedback">
                    패스워드가 일치합니다.
                </div>
                <div class="invalid-feedback">
                    패스워드가 일치하지 않습니다.
                </div>
            </div>

            <div class="d-flex align-items-end flex-column">
                <div class="col alien-end">
                    <button type="submit" id="btn-save" class="btn btn-primary mt-3" disabled>등록</button>
                </div>
            </div>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        </form>
    </div>
</div>
<script src="/js/app/member-signup.js"></script>
{{>layout/footer}}